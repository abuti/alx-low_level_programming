#include "main.h"

/**
 * mul - gives the product of a and b
 * @a: first number
 * @b: second number
 *  
 * Return: returns the multiplication result
 */

int mul(int a, int b)
{
	return (a * b);
}
